# 安全配置

1. `.env`、模型、上传素材、生成产物和任务 JSON 已加入 `.gitignore`。
2. 安装脚本生成随机无痕授权码，并将 `.env` 权限限制为当前用户。
3. ComfyUI 默认仅监听 `127.0.0.1`。
4. API 默认监听 `0.0.0.0:8193`，公网部署需要 TLS、身份验证、限流和请求体大小限制。
5. `H3_API_KEY` 会保护 `/api/v1/*`，当前网页不会自动附加该请求头。网页部署可以在反向代理层完成统一身份验证。
6. 不要将 SSH 密码、云厂商密钥、OpenAI API Key、Hugging Face Token 或无痕授权码提交到 Git。
7. AI 提示词优化请求会把用户输入发送到网页中配置的 OpenAI 兼容服务。使用者需要确认该服务的数据政策。
